This folder contains small practice scripts and sample CSV files used to learn:

PyQGIS (Python scripting inside QGIS)

JavaScript (Node.js) CSV generation

Loading a CSV into QGIS as point features using lon/lat


---------------------------------------------------------------------------------------------------

Files in this folder
CSV Data

stores.csv
Sample stores dataset with lon + lat columns for mapping in QGIS.

storesJS.csv
Same idea as stores.csv, but generated from a JavaScript (Node.js) script.

Python / QGIS (PyQGIS) Notes & Scripts

from qgis.core import (.txt
Notes / example PyQGIS code for loading a CSV as a point layer in QGIS.

import pandas as pd.txt
Notes / example Python code for generating a CSV with pandas.

import pandas as pd and JS.txt, from qgis.core import JS.txt
Mixed notes comparing Python/PyQGIS and JavaScript approaches.

Slides

GIS and Software Programming.pptx
Presentation / notes for the project or class.

Documentation

ReadMe.txt
This file (project instructions / overview).


------------------------------------------------------------------------------------------------------------------

or PyQGIS (QGIS Python scripting)

QGIS 3.x installed

No extra installs needed (QGIS includes Python + PyQGIS)

For JS CSV generation

Node.js installed

Optional: WebStorm for running the script easily